<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aenggukland.letspt.chat.ChatMapper">
    <insert id="makeChatRoom" parameterType="com.aenggukland.letspt.chat.ChatRoom">
        INSERT INTO chat_room (trainer_id, member_id)
        VALUES (#{trainerId}, #{memberId})
    </insert>

    <select id="getMemberChatRoomList" resultType="com.aenggukland.letspt.chat.ChatRoomListResponse" parameterType="Long">
        select DISTINCT ON (cr.chat_room_id) m.name as partnerName, cm.is_read, cm.message_content, cm.sent_at
        from chat_room cr
            INNER JOIN chat_message cm
        ON cr.chat_room_id = cm.chat_room_id
            INNER JOIN member m
            ON cr.trainer_id = m.member_id
        WHERE cr.member_id = #{memberId}
        ORDER BY cr.chat_room_id, sent_at DESC
    </select>

    <select id="getTrainerChatRoomList" resultType="com.aenggukland.letspt.chat.ChatRoomListResponse" parameterType="Long">
        SELECT DISTINCT ON (cr.chat_room_id) m.name AS partnerName, cm.is_read, cm.message_content, cm.sent_at
        FROM chat_room cr
            INNER JOIN chat_message cm
        ON cr.chat_room_id = cm.chat_room_id
            INNER JOIN member m
            ON cr.member_id = m.member_id
        WHERE cr.trainer_id = #{memberId}
        ORDER BY cr.chat_room_id, sent_at DESC;
    </select>

    <select id="getChatDetailList" resultType="com.aenggukland.letspt.chat.ChatDetailResponse">
        SELECT m.name AS senderName, cm.is_read, cm.message_content, cm.sent_at
        FROM chat_message cm
            INNER JOIN member m
            ON cm.sender_id = m.member_id
        WHERE cm.chat_room_id = #{chatRoomId}
    </select>

    <select id="getChatRoomMemberId" parameterType="Long" resultType="Long">
        SELECT member_id
        FROM chat_room
        WHERE chat_room_id = #{chatRoomId}
    </select>

    <select id="getChatRoomTrainerId" parameterType="Long" resultType="Long">
        SELECT trainer_id
        FROM chat_room
        WHERE chat_room_id = #{chatRoomId}
    </select>
</mapper>