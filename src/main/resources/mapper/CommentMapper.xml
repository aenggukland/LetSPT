<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aenggukland.letspt.comment.CommentMapper">
    <select id="getCommentList" parameterType="long" resultMap="com.aenggukland.letspt.comment.Comment">
        SELECT bc.content, bc.created_at, m.name as authorName
        FROM board_comment bc
        INNER JOIN member m
        ON bc.author_id = m.member_id
        WHERE board_id = #{boardId}
          AND is_deleted = FALSE
        ORDER BY created_at ASC
    </select>
</mapper>